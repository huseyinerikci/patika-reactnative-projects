import React from 'react';
import styles from './Loaderstyle';
import { Text, View } from 'react-native';

const Loader = () => {
  return (
    <View>
      <Text style={styles.container}>Loader</Text>
    </View>
  );
};

export default Loader;
